# 新服务器一键部署步骤（图文）

> 配套工具：
> - `linux-deploy.sh` — Linux 一键部署脚本（宝塔/原生双模式，已提交 GitHub）
> - `打包上传.ps1` / `pack_upload.bat` — Windows 本机一键打包上传工具
> - GitHub 仓库：https://github.com/CreativeMarian/ppfish

---

## 一、服务器准备

| 项目 | 要求 |
|------|------|
| 系统 | CentOS 7/8/9、Rocky/AlmaLinux、Ubuntu 20.04+、Debian 11+ |
| 配置 | 4 核 8G 起（Playwright 浏览器 + 4 个服务） |
| 磁盘 | 40G 起（浏览器缓存 + 日志 + 数据库） |
| 安全组 | 放行端口：**9000（前端）、80/443、8089、8090、8091、22** |

> ⚠️ 宝塔模式：服务器需先装好宝塔面板，并在宝塔软件商店安装 **MySQL 8.0 / Redis / Nginx**（版本任意，脚本会复用）。
> 原生模式：什么都不用装，脚本自动装 MySQL/Redis/Nginx。

---

## 二、方式 A：本地打包上传（推荐，国内服务器最快）

### 第1步：本机打包

双击 `pack_upload.bat`，或命令行：

```powershell
powershell -ExecutionPolicy Bypass -File 打包上传.ps1
```

- 会生成 `ppfish-deploy-日期.tar.gz`（自动排除 .git/node_modules/.venv/.env/日志/浏览器缓存）
- 自动包含数据库全量备份 `backups/xianyu_data_full.sql`
- 询问服务器 IP 后自动 scp 上传（输入 root 密码）

> 只打包不传：加 `-NoUpload`；不带数据库备份：加 `-SkipBackup`；不带前端构建产物：加 `-SkipDist`（服务器上重新 npm build）

### 第2步：服务器解压 + 部署

SSH 登录服务器后：

```bash
# 解压到项目目录
mkdir -p /www/wwwroot/ppfish
tar -xzf /tmp/ppfish-deploy-日期.tar.gz -C /www/wwwroot/ppfish
cd /www/wwwroot/ppfish

# 一键部署（交互式，按提示选模式/填密码）
bash linux-deploy.sh --src local --skip-build \
    --backup-sql /www/wwwroot/ppfish/backups/xianyu_data_full.sql \
    --recharge-url http://你的服务器IP:9000
```

> `--skip-build`：包里已带前端 dist，跳过 npm 构建省时间。
> `--backup-sql`：导入你现有的全部商品/卡券/账号数据。
> `--recharge-url`：买家充值页地址，**必须填公网可访问的**（IP 或域名）。

---

## 三、方式 B：GitHub 拉代码部署

```bash
# 安装 git 后拉取（国内网络慢可先手动上传代码包）
git clone --depth 1 https://github.com/CreativeMarian/ppfish.git /opt/ppfish
cd /opt/ppfish

# 一键部署（代码已在本地，选原生模式示例）
bash linux-deploy.sh --mode native --src local \
    --recharge-url http://你的服务器IP:9000
```

> 这种方式没有你的历史数据，部署后是空库（自动建表 + admin/admin123）。

---

## 四、部署脚本运行说明

### 交互式（最推荐，第一次用）

```bash
bash linux-deploy.sh
```

依次会问：
1. 部署模式：宝塔 / 原生（自动检测宝塔，没装宝塔自动用原生）
2. 蜜蜂汇云 APP_KEY / APP_SECRET（在蜜蜂后台「开发者中心/对接文档」查看）
3. 充值页公网地址（默认自动探测公网 IP + 9000 端口）
4. MySQL/Redis 密码：**留空自动生成并显示**（务必记下）

### 全参数式（自动化/重复部署）

```bash
bash linux-deploy.sh --mode native --src local \
    --db-pass MyDBPass123 --redis-pass MyRedisPass123 \
    --mf-key 蜜蜂KEY --mf-secret 蜜蜂SECRET \
    --recharge-url http://1.2.3.4:9000 \
    --backup-sql /www/wwwroot/ppfish/backups/xianyu_data_full.sql \
    --skip-build --yes
```

---

## 五、脚本自动完成的事情

| # | 步骤 | 说明 |
|---|------|------|
| 1 | 环境检测 | root、系统版本、内存提示 |
| 2 | 安装基础依赖 | git/curl/Xvfb 等；Python 3.11+；Node 18+ |
| 3 | 数据库 | 原生模式自动装 MySQL+Redis+Nginx；建库 `xianyu_data`（utf8mb4）；设 Redis 密码 |
| 4 | 导入备份 | （可选）导入你的全量数据 |
| 5 | 生成 .env | 含 DB/Redis 密码、蜜蜂密钥、充值页地址（权限 600） |
| 6 | Python 依赖 | 三个服务各建 venv + 安装（清华源，含 Playwright/DrissionPage） |
| 7 | 浏览器 | Playwright/Patchright chromium（镜像源）+ 系统依赖 |
| 8 | 前端 | npm install + build（--skip-build 时跳过） |
| 9 | systemd | ppfish-backend/websocket/scheduler + Xvfb 虚拟显示 |
| 10 | Nginx | 9000 端口站点（前端 + /api + WebSocket 反代） |
| 11 | 防火墙 | firewalld/ufw 放行 9000/8089/8090/8091/80/443 |
| 12 | 验证 | 三个健康检查 + 前端 200 |

---

## 六、部署完成验证

```bash
curl http://127.0.0.1:8089/health    # {"success":true,...}
curl http://127.0.0.1:8090/health
curl http://127.0.0.1:8091/health
curl -I http://127.0.0.1:9000       # 前端 HTTP 200
```

浏览器访问 `http://服务器IP:9000` → 登录 **admin / admin123** → **立即修改密码**

核对：
- [ ] 商品管理能看到你的全部商品
- [ ] 卡券管理能看到蜜蜂直充卡券（`SELECT COUNT(*) FROM xy_cards WHERE type='mf_api'`）
- [ ] 充值页公网地址能打开（买家点击链接能进填手机号页面）
- [ ] 三个 systemd 服务 active：`systemctl status ppfish-backend ppfish-websocket ppfish-scheduler`

---

## 七、常见问题

| 问题 | 解决 |
|------|------|
| 9000 端口外部打不开 | 云安全组放行 9000（脚本只放行服务器本机防火墙） |
| 充值链接买家打不开 | `.env` 的 `MF_RECHARGE_BASE_URL` 改成公网地址后 `systemctl restart ppfish-websocket` |
| 系统繁忙/Redis 报错 | 检查 `.env` 的 `REDIS_PASSWORD` 与服务器 Redis 实际密码一致 |
| Python 3.11 装不上 | CentOS7 需手动编译或换 CentOS8/Ubuntu 22.04+；Ubuntu 可用 `add-apt-repository ppa:deadsnakes/ppa` |
| Playwright 下载失败 | 重跑：`cd backend-web && .venv/bin/python -m playwright install chromium`（已配置 npmmirror 源） |
| 导入备份报错 | 确认 SQL 是完整 dump；可先手动 `mysql -uroot -p密码 xianyu_data < backups/xianyu_data_full.sql` |
| 登录滑块/扫码失败 | 确认 websocket 服务正常（8090 健康检查）；看 `journalctl -u ppfish-websocket -n 50` |

---

*文档配套 linux-deploy.sh v1.0（2026-09）*
