# ppfish 本地部署文档（Windows · 非 Docker 绿色版）

> 本文档基于本机**实际部署验证**编写（2026-09-10），记录从零到服务可用的完整过程，
> 含踩坑说明。目标：不使用 Docker，用绿色版 MySQL/Redis + Python venv 跑起全部服务。

---

## 一、架构与端口

| 服务 | 端口 | 目录 | 说明 |
|------|------|------|------|
| 前端（Vite dev） | 9000 | `frontend/` | 管理界面，代理请求到 8089 |
| backend-web | 8089 | `backend-web/` | 主 API 网关、业务接口 |
| websocket | 8090 | `websocket/` | 闲鱼长连接、扫码登录、消息收发 |
| scheduler | 8091 | `scheduler/` | 定时任务（自动发货、评价、订单拉取、Cookie 刷新） |
| MySQL | 3306 | 见第三节 | 数据库（库名 `xianyu_data`） |
| Redis | 6379 | 见第四节 | 缓存/会话/队列 |

依赖链：MySQL/Redis → backend-web / websocket / scheduler → frontend。

---

## 二、环境要求（本机已具备）

| 项 | 要求 | 本机实测 |
|----|------|----------|
| Python | 3.11+ | 3.14.7 ✅（全依赖可装，见常见问题 Q1） |
| Node.js | 18+ | v22.23.2 ✅ |
| npm | 随 Node | 10.9.8 ✅ |

> 未安装项：MySQL、Redis（下方绿色版方案）、git（不需要，源码已在本地）。

---

## 三、准备 MySQL 8（绿色版，免安装）

**下载**（约 224MB）：

```
https://mirrors.huaweicloud.com/mysql/Downloads/MySQL-8.0/mysql-8.0.29-winx64.zip
```

**解压初始化**（路径按实际调整，本机为 `ppfish-dev-env`）：

```powershell
# 1. 解压后目录结构：mysql\mysql-8.0.29-winx64\
# 2. 在该目录下创建 my.ini（内容见下）
# 3. 初始化数据目录（root 初始空密码）
cd mysql\mysql-8.0.29-winx64
.\bin\mysqld.exe --defaults-file=.\my.ini --initialize-insecure --console
# 4. 启动
Start-Process .\bin\mysqld.exe -ArgumentList "--defaults-file=.\my.ini","--console" -WindowStyle Hidden
```

`my.ini` 内容（路径含空格时必须用正斜杠，启动参数带引号）：

```ini
[mysqld]
basedir=C:/你的路径/ppfish-dev-env/mysql/mysql-8.0.29-winx64
datadir=C:/你的路径/ppfish-dev-env/mysql/mysql-8.0.29-winx64/data
port=3306
bind-address=127.0.0.1
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
default-authentication-plugin=mysql_native_password
max_connections=200

[client]
port=3306
default-character-set=utf8mb4
```

**建库 + 设置 root 密码**：

```powershell
.\bin\mysql.exe -uroot -e "ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root'; CREATE DATABASE IF NOT EXISTS xianyu_data CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci; FLUSH PRIVILEGES;"
```

验证：`.\bin\mysql.exe -uroot -proot -e "SHOW DATABASES;"` 应看到 `xianyu_data`。

> 应用首次启动会自动建表（本次部署自动创建 65 张表），无需手工执行 SQL。

---

## 四、准备 Redis（Windows 绿色版）

**下载**（约 12MB，GitHub 直连不通时用 ghfast.top 镜像前缀）：

```
https://github.com/tporadowski/redis/releases/download/v5.0.14.1/Redis-x64-5.0.14.1.zip
# 或：https://ghfast.top/https://github.com/tporadowski/redis/releases/download/v5.0.14.1/Redis-x64-5.0.14.1.zip
```

**解压启动**：

```powershell
Expand-Archive Redis-x64-5.0.14.1.zip -DestinationPath redis
Start-Process .\redis\redis-server.exe -ArgumentList "--port","6379" -WindowStyle Hidden
```

验证：`Test-NetConnection 127.0.0.1 -Port 6379` 应 `True`。

> 说明：该 Windows 版为 Redis 5.0，项目 README 建议 6+，但项目实际只用基础命令
> （缓存/会话/队列），5.0 完全够用。如需 6+ 可自行替换其他 Windows 构建版。

---

## 五、项目配置 `.env`

在项目根目录创建 `.env`（本机已创建，内容如下）：

```env
ENVIRONMENT=development
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=xianyu_data
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
CORS_ORIGINS=*
BACKEND_WEB_PORT=8089
WEBSOCKET_PORT=8090
SCHEDULER_PORT=8091
WEBSOCKET_SERVICE_URL=http://127.0.0.1:8090
SCHEDULER_SERVICE_URL=http://127.0.0.1:8091
BACKEND_WEB_SERVICE_URL=http://127.0.0.1:8089
STATIC_DIR=static
TZ=Asia/Shanghai
```

> `JWT_SECRET_KEY`、`INTERNAL_API_TOKEN` 由数据库首次启动自动生成并持久化，无需配置。

---

## 六、安装后端依赖（三个服务）

每个服务独立 venv（本项目实测 Python 3.14.7 全部依赖可装）：

```powershell
# Backend-Web（端口 8089）
cd backend-web
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple

# WebSocket（端口 8090）
cd ..\websocket
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple
# ⚠️ 重要：websocket 与 scheduler 的 pyproject 漏声明了 common 共享模块需要的包，必须补装：
.\.venv\Scripts\python.exe -m pip install bcrypt passlib "python-jose[cryptography]" pycryptodome qrcode email-validator DrissionPage -i https://pypi.tuna.tsinghua.edu.cn/simple

# Scheduler（端口 8091）
cd ..\scheduler
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple
.\.venv\Scripts\python.exe -m pip install bcrypt passlib "python-jose[cryptography]" pycryptodome qrcode email-validator DrissionPage -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> 不补装 bcrypt 会报 `No module named 'bcrypt'` 启动失败（本次部署已踩坑）。
> 若使用 PyPI 官方源慢，可全程加 `-i https://pypi.tuna.tsinghua.edu.cn/simple`。

---

## 七、安装浏览器（Playwright / Patchright）

浏览器文件约 300MB+，国内走 npmmirror 镜像加速：

```powershell
$env:PLAYWRIGHT_DOWNLOAD_HOST="https://cdn.npmmirror.com/binaries/playwright"
cd backend-web
.\.venv\Scripts\python.exe -m playwright install chromium
.\.venv\Scripts\python.exe -m patchright install chromium
```

> websocket/scheduler 的 venv 中同样已装 playwright/patchright 包（六节已装），
> 浏览器二进制为全局共享，只需装一次。

---

## 八、安装前端依赖

```powershell
cd frontend
npm install --registry=https://registry.npmmirror.com --no-audit --no-fund
```

---

## 九、启动服务

### 方式 A：手动启动（本机已验证）

```powershell
# 1. 三个后端（各自窗口/后台任务，日志输出到文件）
cd backend-web;  .\.venv\Scripts\python.exe main.py   # 8089
cd ..\websocket; .\.venv\Scripts\python.exe main.py   # 8090
cd ..\scheduler; .\.venv\Scripts\python.exe main.py   # 8091

# 2. 前端
cd ..\frontend; npm run dev                           # 9000
```

> 注意：启动命令不要接 `Select-Object -First N` 之类的管道截断，会杀掉服务进程。

### 方式 B：GUI 启动器（可选）

项目自带 Windows 启动器，可一键配置并拉起服务：

```powershell
# 先给启动器装依赖（可复用 backend-web 的 venv）
..\backend-web\.venv\Scripts\python.exe -m pip install loguru playwright pymysql redis pillow
# 运行启动器
..\backend-web\.venv\Scripts\python.exe launcher\main.py
```

### 验证

```powershell
Invoke-WebRequest http://127.0.0.1:8089/health   # 期望 {"success":true,...}
Invoke-WebRequest http://127.0.0.1:8090/health
Invoke-WebRequest http://127.0.0.1:8091/health
# 浏览器打开 http://localhost:9000
```

本次部署验证结果：8089 / 8090 / 8091 全部 HTTP 200（`success:true`），9000 前端 200，数据库 65 张表自动创建完毕。

---

## 十、首次使用

1. 浏览器打开 **http://localhost:9000**
2. 登录：默认管理员 `admin` / `admin123`（**登录后立即在设置里改密码**）
3. 「账号管理」→ 添加闲鱼账号 → 扫码登录（websocket 服务会拉起浏览器）
4. 「自动回复规则」配置关键词回复，用小号发消息验证

> ⚠️ 安全与合规提醒：
> - 闲鱼账号密码为明文入库（项目既有设计），本地库务必不要暴露公网
> - 自动化操作违反闲鱼平台规则，存在封号风险，**只用不重要的测试小号**
> - 平台检测是动态对抗，不存在可靠的"防检测"方案，请勿投入真实经营账号

---

## 十一、停止与重启

```powershell
# 停止：结束对应进程（或关掉启动窗口）
Get-Process | Where-Object { $_.ProcessName -match 'python|node' } | Stop-Process -Force   # 谨慎：按需精确匹配
# MySQL/Redis 停止
Stop-Process -Name mysqld -Force; Stop-Process -Name redis-server -Force

# 重启：重新执行第九节命令即可（数据持久化在 data 目录，不会丢失）
```

---

## 十二、常见问题

| 问题 | 原因与解决 |
|------|-----------|
| `No module named 'bcrypt'` 启动失败 | websocket/scheduler 缺 common 依赖，按第六节补装 |
| `mysqld: Failed to open required defaults file` | my.ini 路径含空格时，`--defaults-file` 参数必须带引号，且 ini 内路径用正斜杠 |
| 端口被占用 | `netstat -ano \| findstr 8089` 查占用进程；改 `.env` 中端口并重启 |
| Playwright 下载慢/失败 | 设 `PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright` 重试 |
| npm install 慢 | 加 `--registry=https://registry.npmmirror.com` |
| DrissionPage 未安装警告 | 滑块兜底引擎不可用，不影响主流程；需要滑块对抗可 `pip install DrissionPage` |
| 数据库表没有自动创建 | 确认 `.env` 的 MYSQL_* 与库名 `xianyu_data` 正确，三个后端启动日志应显示"数据库连接成功" |
| Python 3.14 装依赖报编译错误 | 本项目依赖在 3.14 已有预编译 wheel（asyncmy 0.2.14 等）；若个别包编译失败，安装 Python 3.12 重试 |

---

## 附：本机实际部署路径记录

- 项目：`C:\Users\Calvin\AppData\Local\Doubao\User Data\Profile 1\.doubao\agent_mode\workspace\xianyu-auto-reply-src\ppfish`
- 绿色版环境：`C:\Users\Calvin\AppData\Local\Doubao\User Data\Profile 1\.doubao\agent_mode\workspace\ppfish-dev-env\`
  - MySQL：`mysql\mysql-8.0.29-winx64\`（`my.ini` 在其中，数据在 `data\`）
  - Redis：`redis\`
  - 下载缓存：`downloads\`
  - 服务日志：`logs\`（backend-web.log / websocket.log / scheduler.log）
- 前端 dev：`http://localhost:9000`（Vite）
